// Generated by CoffeeScript 1.12.6
(function() {
  var App, CompNode, Dashboard, DataTree, trees;

  trees = require('../two-trees');

  DataTree = trees.DataTree;

  CompNode = trees.CompNode;

  Dashboard = require('./dashboard');


  /*
  model = new DataTree
      tasks:    []
      numDone:  0
      numTotal: 0
      filter:   'all'
  
  
  app = new AppView
      __i__:
          tree: model
  
  
  app.appendTo document.querySelector '.app'
   */

  App = (function() {
    function App() {}

    App.prototype.setData = function(data) {
      this.data = data;
      this.model = new DataTree(this.data);
      this.dashboard = new Dashboard({
        data: this.data
      });
      return this.dashboard.appendTo(document.getElementById('dashboard'));
    };

    App.prototype.update = function(data) {
      this.data = data;
      this.model.update(this.data);
      return this.dashboard.update();
    };

    App.prototype.undo = function() {
      this.model.undo();
      return this.dashboard.update();
    };

    App.prototype.redo = function() {
      this.model.redo();
      return this.dashboard.update();
    };

    return App;

  })();

  window.app = new App();

}).call(this);

//# sourceMappingURL=main.js.map
