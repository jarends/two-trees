// Generated by CoffeeScript 1.12.6
(function() {
  var AppView, CompNode, DataTree, Node, ViewTree, app, model, trees;

  trees = require('../two-trees');

  AppView = require('./app-view');

  ViewTree = trees.ViewTree;

  DataTree = trees.DataTree;

  CompNode = trees.CompNode;

  Node = trees.Node;

  ViewTree.DEFAULT_CLASS = CompNode;

  model = new DataTree({
    tasks: [],
    numDone: 0,
    numTotal: 0,
    filter: 'all'
  });

  app = new Node({
    tag: 'div',
    title: 'my title',
    disabled: false,
    children: [
      {
        tag: 'div',
        title: 'div child'
      }
    ]
  });

  app.appendTo(document.querySelector('.app'));

  console.log(app);

  window.model = model;

  window.data = model.root;

}).call(this);

//# sourceMappingURL=main.js.map
